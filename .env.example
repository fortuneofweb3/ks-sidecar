# RPC Connection (Required)
# Use a high-performance RPC for best results (Helius, QuickNode, Alchemy)
RPC_URL="https://api.mainnet-beta.solana.com"

# Helius Turbo Mode (Recommended for Speed)
# Get a free key at https://dev.helius.xyz
# If provided, scanning takes <1s. If missing, it uses a slower crawler.
HELIUS_API_KEY=""

# Operator Identity (Required)
# Path to your Solana Keypair JSON file (id.json)
OPERATOR_KEYPAIR_PATH="./operator-keypair.json"

# Reclamation Settings
# How often to check for accounts (in hours)
MONITOR_INTERVAL_HOURS="24"

# Database (Internal)
# Leave strictly as-is for local persistent storage
LOCAL_DB_PATH="korascan_local.db"
