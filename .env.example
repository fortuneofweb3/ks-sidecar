# Solana RPC Connection (Required)
RPC_URL="https://api.mainnet-beta.solana.com"

# Helius API Key (Optional but Recommended)
# If provided, scanning uses Helius Enhanced Transactions for speed.
HELIUS_API_KEY=""

# Operator Identity (Required)
# Path to your Solana Keypair JSON file
OPERATOR_KEYPAIR_PATH="./operator-keypair.json"

# Reclamation Settings
# How often to check for accounts (in hours)
MONITOR_INTERVAL_HOURS="24"

# Performance Tuning
# Number of accounts to reclaim in a single transaction (Default: 15)
RECLAIM_BATCH_SIZE="15"
# Priority fee to pay for faster inclusion (MicroLamports, Default: 10000)
PRIORITY_FEE_MICRO_LAMPORTS="10000"

# Safety
# Set to 'true' to simulate reclamation without sending transactions
DRY_RUN="false"

# Database Configuration (Local SQLite)
LOCAL_DB_PATH="korascan_local.db"
